# Depth Anything

Processing pipeline for generating depth maps from regular camera footage using
[Depth Anything](https://github.com/LiheYoung/Depth-Anything).

## Folder structure

```
depth-anything/
├── input/       # source footage (gitignored)
├── output/      # generated depth maps (gitignored)
└── scripts/     # processing helpers (TBD)
```

## Setup

TBD — will document environment setup and model weights here once the pipeline is defined.
